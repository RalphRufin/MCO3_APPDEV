<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= user.name %>'s Profile</title>
    
</head>
<style>

/* General Styles */
@import url('https://fonts.googleapis.com/css2?family=Roboto&display=swap');

body {
    font-family: 'Roboto', sans-serif;
    background-color: #f4f4f4;
    margin: 0;
    padding: 0;
}

.profile-edit-container {
    background-color: #fff;
    margin: 20px auto;
    padding: 20px;
    width: 80%;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    display: flex;
    flex-direction: column;
}

.profile-edit-header {
    text-align: center;
    margin-bottom: 20px;
}

.profile-edit-form {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.profile-edit-form label {
    display: block;
    margin-bottom: 5px;
}

.profile-edit-form input[type="file"],
.profile-edit-form textarea {
    width: 100%;
    padding: 10px;
    margin-bottom: 20px;
    border-radius: 4px;
    border: 1px solid #ddd;
}

.profile-edit-form button.save {
    background-color: #FF884D;
    color: #fff;
    border: none;
    padding: 10px 20px;
    border-radius: 4px;
    cursor: pointer;
    margin-bottom: 10px;
}

.profile-edit-form button.save:hover {
    background-color: #ff884a;
}
.save {
    text-decoration: none;
    color: #FF884D;
    padding: 10px 20px;
    border-radius: 4px;
    background-color: #FF884a;
    color: #fff;
    border: none;
    cursor: pointer;
    display: inline-block;
    margin: 5px;
}
.delete-profile-button {
    text-decoration: none;
    color: #FF884D;
    padding: 10px 20px;
    border-radius: 4px;
    background-color: #FF884a;
    color: #fff;
    border: none;
    cursor: pointer;
    display: inline-block;
    max-width: 50px;
    margin: 5px;
}

.delete-profile-button:hover {
    background-color: #0056b3;
}

.dropbtn {
    background-color: #FF884D; /* Change background color to dark orange */
    color: white;
    padding: 16px;
    font-size: 16px;
    border: none;
    cursor: pointer;
    float: right; /* Move the menu button to the right */
}

.dropdown {
    position: relative;
    display: inline-block;
}

.profile-dropdown {
    display: none;
    position: absolute;
    background-color: #FF884D;
    min-width: 160px;
    box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
    z-index: 1;
    top: 100%; 
    left: 0; 
}
.pfp {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    cursor: pointer;
    display: block;
    margin: auto; /* Center the image horizontally */
}

.dropdown-content a {
    color: white;
    padding: 12px 16px;
    text-decoration: none;
    display: block;
    font-weight: bold;
}

.dropdown-content a:hover {
    background-color: lightsalmon; 
}

.dropdown:hover .profile-dropdown {
    display: block;
}
</style>
<body>
    <div class="menu"> 
        <div class="menu-header"> 
            <a href="/labs">
                <img class="logo" src="../imgs/LabrYnth.png" />
            </a>
            <div class="dropdown">
    <button class="dropbtn">Menu</button>
    <div class="profile-dropdown">
        <img class="pfp" src="<%= user.jpgFilename %>" />
        <div class="dropdown-content">
            <a href="<%= user.role === 'student' ? '/studentLabs' : '/technicianLabs' %>">Labs</a>
            <a href="/user/<%= user._id %>">My Profile</a>
            <a href="<%= user.role === 'student' ? '/student-reservation/' + user._id : '/technician-reservation/' + user._id %>">My Reservations</a>
            <a href="/search/<%= user._id %>">Search</a>
            <a href="/labs">Logout</a>
        </div>
    </div>
  </div>
        </div>  
    </div>
    <div class="profile-edit-container">
        <div class="profile-edit-header">
            <h1>Edit <%= user.name %>'s Profile</h1>
        </div>
        <form action="/user/edit/<%= user._id %>" method="POST" enctype="multipart/form-data">
            <label for="description">Description:</label>
            <textarea name="description" rows="4" placeholder="Enter a description"><%= user.txtFilename %></textarea>
            <br>
            <label for="image">Profile Image:</label>
            <input type="file" id="image" name="image">
            <br>
            <button type="submit" class="save">Save Changes</button>
        </form>
        <a href="/user/delete/<%= user._id %>" class="delete-profile-button">Delete Profile</a>
    </div>
</body>
</html>
