<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.name %>'s Profile</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .profile-container {
            background-color: #fff;
            margin: 20px auto;
            padding: 20px;
            width: 80%;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        .profile-header {
            text-align: center;
            margin-bottom: 20px;
        }
        .profile-info {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .profile-info p {
            margin-bottom: 10px;
        }
        .profile-info a {
            text-decoration: none;
            color: #007BFF;
            padding: 10px 20px;
            border-radius: 4px;
            background-color:  rgba(255, 145, 77, 1);
            color: #fff;
            border: none;
            cursor: pointer;
        }
        .profile-info a:hover {
            background-color: #0056b3;
        }

        /* Container to maintain aspect ratio of profile image */
        .profile-image-container {
            width: 200px; /* Adjust the width as needed */
            height: auto; /* Automatically adjust height to maintain aspect ratio */
            margin-bottom: 20px;
        }

        /* Profile image with max width and height to fit within container */
        .profile-image {
            max-width: 100%;
            max-height: 100%;
            display: block;
            margin: 0 auto; /* Center the image horizontally */
        }
    </style>
</head>
<body>
    <div class="menu"> 
        <div class="menu-header"> 
            <a href="/labs">
                <img class="logo" src="../imgs/LabrYnth.png" />
            </a>
            <div class="dropdown">
    <button class="dropbtn">Menu</button>
    <div class="profile-dropdown">
        <img class="pfp" src="<%= user.jpgFilename %>" />
        <div class="dropdown-content">
            <a href="/roleredirect/<%= user._id %>">Labs</a>
            <a href="/user/edit/<%= user._id %>">Edit Profile</a>
            <a href="/roleredirectres/<%= user._id %>">My Reservations</a>
            <a href="/search">Search</a>
            <a href="/labs">Logout</a>
        </div>
    </div>
  </div>
        </div>  
    </div>
    <div class="profile-container">
        <div class="profile-header">
            <h1><%= user.name %>'s Profile</h1>
        </div>
        <div class="profile-info">
            
            <div class="profile-image-container">
                <img src="/<%= user.jpgFilename %>" alt="Profile Image" class="profile-image">
            </div>
            
            
            <p>Email: <%= user.email %></p>
            <p>Role: <%= user.role %></p>
            <p>Description: <%= user.txtFilename %></p>
            
            <a href="/search">Back to Home</a>
        </div>
    </div>
</body>
</html>